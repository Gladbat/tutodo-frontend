import { LaziLoadImage } from './lazi-load-image';

describe('LaziLoadImage', () => {
  it('should create an instance', () => {
    const directive = new LaziLoadImage();
    expect(directive).toBeTruthy();
  });
});
